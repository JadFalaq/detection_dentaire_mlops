inference:
  checkpoint: models/checkpoints/champion/weights/best.pt
  image_size: 640
  conf_threshold: 0.25
  iou_threshold: 0.50
  max_det: 300
  device: cpu
  save_txt: false
  save_conf: true

outputs:
  save_dir: reports/predictions/inference
